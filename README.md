# EmotionCorrector
Sounds worse than it actually is

Turned out worse than it sounds because I did not have a water pump and so used a lser and now it looks like I am trying to automate snipers

I promise I did this out of boredom and cursiosity



Notes:
Don't run arduino IDE and your python IDE at the same time since the port is bidirectional regardless of whether we only write in the python script and only read on the arduino.
We only need the python IDE to be on and running the script, the arduino reads regardless of whether the IDE is on.
I'm pretty sure I got the maths wrong but that's purely because my project is a bunch of duct tape for every single component. If I measured everything properly it would probably have been fine.
